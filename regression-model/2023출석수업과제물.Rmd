# 회귀모형 2023 출석수업과제물

１번. 연습문제 1장 1번(p. 39) 자료를 이용하여 1.7 분석사례와 같이 분석하고, 설명하시오.

X = c(3, 1, 5, 8, 1, 4, 2, 6, 9, 3, 5, 7, 2, 6) Y = c(39, 24, 115, 105, 50, 86, 67, 90, 140, 112, 70, 186, 43, 126) (n = length(X)) \# 두 변수의 마지막 3개의 값을 확인 tail(cbind(X, Y), 3) \# 산점도를 그린다. plot(X, Y, pch=19, main="기계의 사용연도와 정비비용의 산점도", xlab = "사용연도 (단위:년)", ylab = "정비비용 (단위:1000원)")

# 회귀모형 적합

machine.lm = lm(Y\~X)

# 추정회귀식

paste0("hat_Y =", round(coef(model)["(Intercept)"],3), " + ",\
round(coef(model)["X"],3),"\*X")

(참고: 교재 수정 p.37 p.x = data.frame(price=c(1:45))

2번. 연습문제 2장 3번(p. 87) 자료를 이용하여 2.8 분석사례와 같이 분석하고, 설명하시오.
